# Bulk Question Upload Tasks

- `[/]` Install parsing dependencies (`xlsx`, `pdf-parse`, `mammoth`)
- `[ ]` Create `/api/questions/upload` route for parsing files and inserting questions
- `[ ]` Update Admin Exam Manager UI with "Bulk Upload Questions" panel
- `[ ]` Test bulk question extraction locally
